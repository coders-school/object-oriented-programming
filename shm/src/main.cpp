#include "game.hpp"

constexpr size_t startMoney = 500;
constexpr size_t gameDays = 365;
constexpr size_t finalGoal = 9999;

int main() {
    // Game game(startMoney, gameDays, finalGoal);
    // game.startGame();

    return 0;
}
