name: task_4

on: [push, pull_request, workflow_dispatch]

jobs:
  task_4:
    runs-on: ubuntu-latest
    env:
      path: homework/task_4/build
    steps:
    - name: Check out code
      uses: actions/checkout@master
    - name: Create build directory
      run: mkdir ${{ env.path }}
    - name: Compile
      working-directory: ${{ env.path }}
      run: |
        cmake ..
        make
    - name: Run tests
      working-directory: ${{ env.path }}
      run: ctest -V